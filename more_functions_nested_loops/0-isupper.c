#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: check chracter
 * Return: 1 if c is uppercase els 0
 */

int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
return (0);
}
