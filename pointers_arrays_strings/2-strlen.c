#include "main.h"
/**
 * _strlen - Returns the length of a string.
 * @s: string
 * Return- String length
 * strlen - Function calculate length of string
 */

int _strlen(char *s)
{
int len = 0;
len = strlen(s);
return (len);
}
